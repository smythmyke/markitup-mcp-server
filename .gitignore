node_modules/
dist/
*.log
.env
.env.local

# Local tools downloaded for one-off tasks
mcp-publisher.exe
mcp-publisher
*.tar.gz

# .mcpb bundles are build artifacts — regen via mcpb pack + Python patch script
*.mcpb
